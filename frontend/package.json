{
  "name": "pocketii-frontend",
  "private": true,
  "description": "Server-rendered Jinja fallback frontend. No build step: plain CSS (static/css/main.css) + vendored JS (static/js/*), icons via vendored lucide (static/js/lucide.min.js)."
}
